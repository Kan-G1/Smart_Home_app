<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SmartHomeApp">

        <!-- MAIN ENTRY POINT: Dashboard via MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Secondary activity (login screen, not launcher) -->
        <activity
            android:name=".AuthActivity"
            android:exported="false" />

        <!-- Background service for sensor simulation -->
        <service
            android:name=".SensorSimulationService"
            android:exported="false" />

    </application>
</manifest>
